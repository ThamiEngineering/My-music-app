<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Vous êtes bannis</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            text-align: center;
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        h1 {
            font-size: 2em;
            color: #f44336;
            margin-bottom: 20px;
        }

        p {
            font-size: 1.2em;
            color: #777;
            margin-bottom: 20px;
        }

        button {
            background-color: #f44336;
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 0.9em;
        }

        button:hover {
            background-color: #d32f2f;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Vous êtes bannis</h1>
        <p>Vous êtes bannis pour le moment. Veuillez contacter l'administrateur pour plus d'informations.</p>
        <form action="{{ path('app_logout') }}" method="post">
            <button type="submit">Déconnexion</button>
        </form>
    </div>
</body>
</html>